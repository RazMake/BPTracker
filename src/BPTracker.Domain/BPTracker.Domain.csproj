<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>BPTracker.Domain</RootNamespace>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <!-- The domain is the innermost layer: it must never take a dependency.
       Adding a PackageReference or ProjectReference here requires an ADR. -->

</Project>
